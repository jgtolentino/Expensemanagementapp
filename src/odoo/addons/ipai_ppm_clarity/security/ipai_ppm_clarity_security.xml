<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <data noupdate="1">
        <!-- Security Groups -->
        <record id="group_clarity_canvas_editor" model="res.groups">
            <field name="name">Clarity Canvas Editor</field>
            <field name="category_id" ref="base.module_category_project_management"/>
            <field name="implied_ids" eval="[(4, ref('project.group_project_user'))]"/>
            <field name="comment">Users in this group can create and edit canvas widgets</field>
        </record>

        <record id="group_clarity_scheduler" model="res.groups">
            <field name="name">Clarity Autoschedule Manager</field>
            <field name="category_id" ref="base.module_category_project_management"/>
            <field name="implied_ids" eval="[(4, ref('project.group_project_manager'))]"/>
            <field name="comment">Users in this group can run autoschedule and manage critical path</field>
        </record>

        <!-- Record Rules -->
        <!-- Phase: Users can see phases of projects they have access to -->
        <record id="phase_rule_user" model="ir.rule">
            <field name="name">Phase: Project Members</field>
            <field name="model_id" ref="model_ipai_project_phase"/>
            <field name="domain_force">[('project_id.message_partner_ids', 'in', [user.partner_id.id])]</field>
            <field name="groups" eval="[(4, ref('project.group_project_user'))]"/>
        </record>

        <!-- Canvas: Users can see canvases of their projects -->
        <record id="canvas_rule_user" model="ir.rule">
            <field name="name">Canvas: Project Members</field>
            <field name="model_id" ref="model_ipai_canvas"/>
            <field name="domain_force">[('project_id.message_partner_ids', 'in', [user.partner_id.id])]</field>
            <field name="groups" eval="[(4, ref('project.group_project_user'))]"/>
        </record>

        <!-- Canvas View: Users can only edit their own views -->
        <record id="canvas_view_rule_own" model="ir.rule">
            <field name="name">Canvas View: Own or Shared</field>
            <field name="model_id" ref="model_ipai_canvas_view"/>
            <field name="domain_force">['|', ('user_id', '=', user.id), ('is_shared', '=', True)]</field>
            <field name="groups" eval="[(4, ref('project.group_project_user'))]"/>
            <field name="perm_read" eval="True"/>
            <field name="perm_write" eval="True"/>
            <field name="perm_create" eval="True"/>
            <field name="perm_unlink" eval="True"/>
        </record>

        <!-- To-Do: Users can see to-dos of tasks they can access -->
        <record id="todo_rule_user" model="ir.rule">
            <field name="name">To-Do: Task Access</field>
            <field name="model_id" ref="model_ipai_task_todo"/>
            <field name="domain_force">[('project_id.message_partner_ids', 'in', [user.partner_id.id])]</field>
            <field name="groups" eval="[(4, ref('project.group_project_user'))]"/>
        </record>

    </data>
</odoo>
